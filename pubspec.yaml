name: leafscan_app
description: PlantCare AI - Detect. Diagnose. Cure.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  go_router: ^13.0.0
  image_picker: ^1.0.7
  http: ^1.2.0
  share_plus: ^10.1.2
  shared_preferences: ^2.3.2
  http_parser: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true